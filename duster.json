{
    "include": [
        "src",
        "tests"
    ],
    "exclude": [
        "tests/fixtures"
    ],
    "scripts": {
        "lint": {
            "phpstan": ["./vendor/bin/phpstan", "analyse", "--memory-limit=512M"]
        }
    },
    "processTimeout": 120
}